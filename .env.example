# Proxy URL used by this container to connect to the upstream proxy.
# Note: proxy_user and proxy_password must be URL-encoded.
# Example: user "alice@example.com" -> "alice%40example.com"
# Example: password "p@ss:word" -> "p%40ss%3Aword"
HTTP_PROXY_FOR_PROXY=http://<proxy_user>:<proxy_password>@<proxy_host>:<proxy_port>
# Host port that binds to container port 8080.
HOST_BIND_PORT=8080
